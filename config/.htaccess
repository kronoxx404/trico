# Bloquear TODO acceso HTTP directo a este directorio.
# Los archivos aquí solo pueden ser incluidos por PHP internamente.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
